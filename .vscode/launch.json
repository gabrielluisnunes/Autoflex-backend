{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "java",
      "name": "Autoflex Backend (MySQL)",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "mainClass": "com.autoflex.backend.AutoflexApplication",
      "projectName": "backend",
      "preLaunchTask": "autoflex: clear backend ports",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
